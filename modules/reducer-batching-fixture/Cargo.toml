[package]
name = "reducer-batching-fixture"
version = "0.0.0"
edition.workspace = true
license-file = "LICENSE"

[lib]
crate-type = ["cdylib"]
bench = false

[dependencies]
spacetimedb = { path = "../../crates/bindings", features = ["unstable"] }

log.workspace = true
